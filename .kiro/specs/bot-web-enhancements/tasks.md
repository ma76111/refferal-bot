# خطة التنفيذ: bot-web-enhancements

## نظرة عامة

تنفيذ تدريجي لمجموعة شاملة من التحسينات تشمل: نظام الإشعارات، دعم اللغات الإضافية، أمان الأجهزة، نظام التذاكر، إدارة المهام المتقدمة، وتحسينات الواجهة الويب.

## المهام

- [x] 1. إعداد قاعدة البيانات والجداول الجديدة
  - إضافة الجداول الجديدة إلى `database.js`: `notifications`، `notification_prefs`، `tickets`، `ticket_messages`، `device_logs`، `activity_log`، `task_audit`
  - إضافة الأعمدة الجديدة للجداول الموجودة: `paused_at` و`country_code` للمهام، `notification_channel` و`last_known_ip` و`country_code` للمستخدمين، `wallet_address` و`network` للإيداعات، `ton_address` للسحوبات
  - إضافة الفهارس الجديدة المذكورة في التصميم على أعمدة `status` و`created_at` و`user_id` لجميع الجداول ذات الصلة
  - _المتطلبات: 6.1، 6.2_


- [x] 2. تثبيت المكتبات الجديدة وإعداد البنية الأساسية
  - تثبيت `express-rate-limit` و`helmet` في `web/server/`
  - تثبيت `fast-check` و`jest` كـ devDependencies في المشروع الرئيسي
  - إنشاء مجلد `web/server/middleware/` وملفات `rateLimit.js` و`security.js`
  - إنشاء مجلد `web/client/src/i18n/` وملف `translations.js` الأساسي (مفاتيح فارغة)
  - إنشاء مجلد `web/client/src/components/` وملفات `Toast.jsx` و`Spinner.jsx` و`Table.jsx`
  - _المتطلبات: 6.4، 6.8، 11.2، 11.5_

- [x] 3. تطبيق Rate Limiting وSecurity Headers
  - [x] 3.1 إنشاء `web/server/middleware/rateLimit.js` مع `publicLimiter` (100/دقيقة لكل IP) و`protectedLimiter` (30/دقيقة لكل مستخدم)
    - _المتطلبات: 6.4_
  - [x] 3.2 إنشاء `web/server/middleware/security.js` لإعداد `helmet` مع HSTS وCSP وX-Frame-Options
    - _المتطلبات: 6.8_
  - [x] 3.3 تطبيق الـ middlewares في `web/server/index.js` على جميع المسارات
    - _المتطلبات: 6.4، 6.8_
  - [ ]* 3.4 كتابة اختبار خاصية لـ Rate Limiting
    - **الخاصية 15: تطبيق Rate Limiting على المسارات العامة**
    - **يتحقق من: المتطلب 6.4**


- [x] 4. نقطة تفتيش — التحقق من تمرير جميع الاختبارات
  - تأكد من تمرير جميع الاختبارات، اسأل المستخدم إذا ظهرت أي تساؤلات.

- [x] 5. توسيع نظام اللغات (المتطلبان 2 و5)
  - [x] 5.1 توسيع `languages.js` بإضافة مفاتيح اللغات الثلاث الجديدة: الفارسية (`fa`)، التركية (`tr`)، الأوردو (`ur`) لجميع المفاتيح الموجودة
    - نسخ هيكل كائن `ar` لكل لغة جديدة وترجمة القيم
    - _المتطلبات: 2.1، 2.4، 2.6، 5.1_
  - [ ]* 5.2 كتابة اختبار خاصية لاكتمال مفاتيح الترجمة
    - **الخاصية 3: اكتمال مفاتيح الترجمة في جميع اللغات**
    - **يتحقق من: المتطلبات 2.1، 2.6، 5.5**
  - [ ]* 5.3 كتابة اختبار خاصية لـ Fallback إلى العربية
    - **الخاصية 4: الـ Fallback إلى العربية عند غياب مفتاح**
    - **يتحقق من: المتطلب 2.3**
  - [ ]* 5.4 كتابة اختبار خاصية لثبات المصطلحات التقنية
    - **الخاصية 5: ثبات المصطلحات التقنية عبر الترجمات**
    - **يتحقق من: المتطلب 2.4**
  - [x] 5.5 إنشاء `web/client/src/i18n/translations.js` يحتوي على نفس المفاتيح بالست لغات للواجهة الويب
    - _المتطلبات: 5.2، 2.1_
  - [x] 5.6 تحديث `web/client/src/index.css` لإضافة دعم RTL (الفارسية والأوردو والعربية) ودعم LTR لباقي اللغات
    - _المتطلبات: 2.5_


- [x] 6. نموذج NotificationPrefs ونظام الإشعارات الأساسي (المتطلبان 1 و9)
  - [x] 6.1 إنشاء `models/NotificationPrefs.js` مع دوال: `getPrefs(userId)`، `savePrefs(userId, prefs)`، `isEnabled(userId, type)`
    - _المتطلبات: 1.1، 1.2_
  - [ ]* 6.2 كتابة اختبار خاصية لاتساق تفضيلات الإشعارات
    - **الخاصية 1: الاتساق التام لتفضيلات الإشعارات**
    - **يتحقق من: المتطلبات 1.1، 1.2**
  - [x] 6.3 إنشاء `models/Notification.js` مع دوال: `create(userId, type, title, body, link)`، `getUserNotifications(userId)`، `markRead(id, userId)`، `markAllRead(userId)`، `getUnreadCount(userId)`، `cleanExpired()`
    - _المتطلبات: 9.1، 9.2، 9.3، 9.4_
  - [ ]* 6.4 كتابة اختبار خاصية لاتساق عداد الإشعارات غير المقروءة
    - **الخاصية 24: اتساق عداد الإشعارات غير المقروءة**
    - **يتحقق من: المتطلبات 9.2، 9.5**
  - [ ]* 6.5 كتابة اختبار خاصية لتعليم الإشعار مقروءاً
    - **الخاصية 25: تعليم الإشعار مقروءاً — round-trip**
    - **يتحقق من: المتطلب 9.3**
  - [x] 6.6 إنشاء دالة `sendNotification(userId, type, payload)` في `handlers/notificationHandler.js` تتحقق من التفضيلات قبل الإنشاء
    - _المتطلبات: 1.3، 1.4_
  - [ ]* 6.7 كتابة اختبار خاصية لعدم إرسال الإشعارات المعطّلة
    - **الخاصية 2: عدم إرسال الإشعارات المعطّلة**
    - **يتحقق من: المتطلبات 1.3، 1.4**


- [x] 7. مسارات API للإشعارات
  - [x] 7.1 إنشاء `web/server/routes/notifications.js` مع المسارات: `GET /api/notifications`، `POST /api/notifications/:id/read`، `POST /api/notifications/read-all`، `GET /api/notifications/prefs`، `PUT /api/notifications/prefs`، `GET /api/notifications/unread-count`
    - _المتطلبات: 9.1، 9.2، 9.3، 9.5_
  - [x] 7.2 ربط مسار الإشعارات في `web/server/index.js`
    - _المتطلبات: 9.1_
  - [ ]* 7.3 كتابة اختبار خاصية لانتهاء صلاحية الإشعارات
    - **الخاصية 26: انتهاء صلاحية الإشعارات بعد 30 يوماً**
    - **يتحقق من: المتطلب 9.4**

- [x] 8. نقطة تفتيش — التحقق من تمرير جميع الاختبارات
  - تأكد من تمرير جميع الاختبارات، اسأل المستخدم إذا ظهرت أي تساؤلات.

- [x] 9. نظام تسجيل الأجهزة (المتطلب 3)
  - [x] 9.1 إنشاء `web/server/middleware/deviceLogger.js` يسجل IP وUser-Agent وfingerprint عند كل جلسة
    - _المتطلبات: 3.1، 3.6_
  - [ ]* 9.2 كتابة اختبار خاصية لتسجيل بصمة الجهاز
    - **الخاصية 6: تسجيل بصمة الجهاز في كل جلسة**
    - **يتحقق من: المتطلبات 3.1، 3.6**
  - [x] 9.3 تحديث مسار المصادقة `web/server/routes/auth.js` لاستدعاء deviceLogger وفحص الأجهزة المحظورة
    - _المتطلبات: 3.2، 3.3_
  - [ ]* 9.4 كتابة اختبار خاصية لرفض الأجهزة المحظورة
    - **الخاصية 7: رفض تسجيل الأجهزة المحظورة**
    - **يتحقق من: المتطلب 3.3**
  - [x] 9.5 إضافة مسارات إدارية في `web/server/routes/admin.js`: `GET /api/admin/device-logs`، `GET /api/admin/duplicate-accounts`، `POST /api/admin/override-ip/:userId`
    - _المتطلبات: 3.4، 3.5، 3.8_
  - [ ]* 9.6 كتابة اختبار خاصية لتأشير الحسابات المتعددة
    - **الخاصية 8: تأشير الحسابات المتعددة لنفس الـ IP**
    - **يتحقق من: المتطلب 3.4**


- [x] 10. نظام تذاكر الدعم — الجانب الخلفي (المتطلب 4)
  - [x] 10.1 إنشاء `models/Ticket.js` مع دوال: `create(userId, subject, priority)`، `getById(id)`، `getUserTickets(userId)`، `addMessage(ticketId, senderId, body, isAdmin)`، `getMessages(ticketId)`، `updateStatus(id, status)`، `assign(id, adminId)`، `getOverdue(hours)`
    - دالة `create` تولّد `ticket_no` فريداً بصيغة `TKT-XXXXXXXX`
    - _المتطلبات: 4.1، 4.2، 4.3، 4.5_
  - [ ]* 10.2 كتابة اختبار خاصية لفريدية أرقام التذاكر
    - **الخاصية 10: فريدية أرقام تذاكر الدعم**
    - **يتحقق من: المتطلب 4.2**
  - [ ]* 10.3 كتابة اختبار خاصية لدعم مستويات الأولوية الأربعة
    - **الخاصية 11: دعم مستويات الأولوية الأربعة للتذاكر**
    - **يتحقق من: المتطلب 4.3**
  - [ ]* 10.4 كتابة اختبار خاصية لبقاء محادثة التذكرة المغلقة
    - **الخاصية 12: بقاء محادثة التذكرة المغلقة**
    - **يتحقق من: المتطلب 4.5**
  - [x] 10.5 إنشاء `web/server/routes/tickets.js` مع المسارات: `POST /api/tickets`، `GET /api/tickets`، `GET /api/tickets/:id`، `POST /api/tickets/:id/reply`، `GET /api/admin/tickets`، `POST /api/admin/tickets/:id/reply`، `PUT /api/admin/tickets/:id/status`، `PUT /api/admin/tickets/:id/assign`
    - _المتطلبات: 4.7، 4.8_
  - [ ]* 10.6 كتابة اختبار خاصية لعزل تذاكر المستخدمين
    - **الخاصية 13: عزل تذاكر المستخدمين**
    - **يتحقق من: المتطلب 4.7**
  - [x] 10.7 ربط مسار التذاكر في `web/server/index.js` وإضافة منطق إشعار المشرف عند تجاوز 48 ساعة (job دوري)
    - _المتطلبات: 4.4، 4.6_


- [x] 11. نقطة تفتيش — التحقق من تمرير جميع الاختبارات
  - تأكد من تمرير جميع الاختبارات، اسأل المستخدم إذا ظهرت أي تساؤلات.

- [x] 12. إدارة المهام المتقدمة — الجانب الخلفي (المتطلب 8)
  - [x] 12.1 إنشاء `models/TaskAudit.js` مع دالة `log(taskId, changedBy, fieldName, oldValue, newValue)`
    - _المتطلبات: 8.6_
  - [ ]* 12.2 كتابة اختبار خاصية لتسجيل تدقيق جميع تعديلات المهام
    - **الخاصية 23: تسجيل تدقيق جميع تعديلات المهام**
    - **يتحقق من: المتطلب 8.6**
  - [x] 12.3 تحديث `web/server/routes/tasks.js` بإضافة: `PATCH /api/tasks/:id` (تعديل الحقول المسموحة فقط)، `POST /api/tasks/:id/pause`، `POST /api/tasks/:id/resume`، `GET /api/tasks/:id/audit`
    - رفض تعديل `referral_link` و`reward_per_user` و`required_count` بكود 400
    - _المتطلبات: 8.1، 8.2، 8.3، 8.4، 8.5_
  - [ ]* 12.4 كتابة اختبار خاصية لقبول تعديلات الحقول المسموح بها
    - **الخاصية 20: قبول تعديلات الحقول المسموح بها**
    - **يتحقق من: المتطلب 8.1**
  - [ ]* 12.5 كتابة اختبار خاصية لرفض تعديل الحقول المحمية
    - **الخاصية 21: رفض تعديل الحقول المحمية**
    - **يتحقق من: المتطلب 8.2**
  - [ ]* 12.6 كتابة اختبار خاصية لدورة حياة الإيقاف المؤقت
    - **الخاصية 22: دورة حياة الإيقاف المؤقت للمهام — round-trip**
    - **يتحقق من: المتطلبات 8.3، 8.4، 8.5**
  - [x] 12.7 تحديث استعلام `GET /api/tasks` في `web/server/routes/tasks.js` لاستبعاد المهام المتوقفة مؤقتاً (status = 'paused') وإضافة فلترة country_code
    - _المتطلبات: 8.4، 3.7_
  - [ ]* 12.8 كتابة اختبار خاصية لفلترة المهام حسب البلد
    - **الخاصية 9: فلترة المهام حسب بلد المستخدم**
    - **يتحقق من: المتطلب 3.7**


- [x] 13. سجل الأنشطة والتحسينات الإدارية (المتطلب 7)
  - [x] 13.1 إنشاء `models/ActivityLog.js` مع دالة `log(userId, eventType, metadata)`، `getUserActivity(userId, page, pageSize)`
    - _المتطلبات: 7.3، 7.4، 7.5_
  - [ ]* 13.2 كتابة اختبار خاصية لتسجيل الأنشطة round-trip
    - **الخاصية 17: تسجيل الأنشطة — round-trip**
    - **يتحقق من: المتطلب 7.3**
  - [ ]* 13.3 كتابة اختبار خاصية للترتيب التنازلي لسجل الأنشطة
    - **الخاصية 18: الترتيب التنازلي لسجل الأنشطة**
    - **يتحقق من: المتطلب 7.4**
  - [ ]* 13.4 كتابة اختبار خاصية لـ Pagination
    - **الخاصية 19: Pagination — التحكم في حجم الصفحة**
    - **يتحقق من: المتطلبات 7.5، 11.5**
  - [x] 13.5 إنشاء `web/server/routes/activity.js` مع: `GET /api/user/activity`، `GET /api/user/balance-chart`، `GET /api/user/widgets-config`، `PUT /api/user/widgets-config`
    - _المتطلبات: 7.1، 7.2، 7.5، 7.6_
  - [x] 13.6 ربط سجل الأنشطة بالأحداث الرئيسية: تسجيل الدخول في `auth.js`، وإنشاء المهام والإثباتات والمعاملات المالية في Routes المعنية
    - _المتطلبات: 7.3_

- [x] 14. نقطة تفتيش — التحقق من تمرير جميع الاختبارات
  - تأكد من تمرير جميع الاختبارات، اسأل المستخدم إذا ظهرت أي تساؤلات.


- [x] 15. تحسينات المحافظ والتحقق من المدخلات (المتطلب 10)
  - [x] 15.1 إنشاء `web/server/utils/validators.js` مع دوال للتحقق من: عناوين TRC20، عناوين TON، معرفات Binance Pay، والمبالغ الموجبة
    - _المتطلبات: 10.3، 10.4، 6.5_
  - [ ]* 15.2 كتابة اختبار خاصية للتحقق من صحة عناوين المحافظ
    - **الخاصية 27: التحقق من صحة عناوين المحافظ**
    - **يتحقق من: المتطلبات 10.3، 10.4**
  - [ ]* 15.3 كتابة اختبار خاصية لرفض المدخلات غير الصالحة
    - **الخاصية 16: رفض المدخلات غير الصالحة**
    - **يتحقق من: المتطلبات 6.5، 10.3، 10.4**
  - [x] 15.4 تحديث مسار `web/server/routes/wallet.js` لدعم طرق الإيداع والسحب الثلاث مع التحقق المدمج من validators.js
    - _المتطلبات: 10.1، 10.2، 10.3_
  - [ ]* 15.5 كتابة اختبار خاصية لاتساق حالة المعاملات المالية
    - **الخاصية 28: اتساق حالة المعاملات المالية**
    - **يتحقق من: المتطلب 10.5**

- [ ] 16. اختبارات الأمان والـ SQL Injection
  - [ ]* 16.1 كتابة اختبار خاصية لرفض المدخلات الخبيثة
    - **الخاصية 14: رفض المدخلات الخبيثة (SQL Injection)**
    - **يتحقق من: المتطلب 6.3**


- [x] 17. تحسينات الواجهة الويب — مكونات أساسية (المتطلب 11)
  - [x] 17.1 تطوير `web/client/src/components/Toast.jsx` لعرض رسائل النجاح والخطأ مع اختفاء تلقائي بعد 3 ثوانٍ
    - _المتطلبات: 11.6، 11.7_
  - [x] 17.2 تطوير `web/client/src/components/Spinner.jsx` لعرض مؤشر تحميل عند العمليات التي تتجاوز 300ms
    - _المتطلبات: 11.2_
  - [x] 17.3 تطوير `web/client/src/components/Table.jsx` مع دعم الفرز والتصفية والـ Pagination
    - _المتطلبات: 11.5_
  - [x] 17.4 تحديث `web/client/src/index.css` لإضافة متغيرات CSS للألوان ودعم Dark/Light mode وتصميم Responsive
    - _المتطلبات: 11.1، 11.3، 11.4_
  - [x] 17.5 تحديث `web/client/src/ThemeContext.jsx` لحفظ تفضيل الثيم في localStorage وتطبيقه عند تحميل الصفحة
    - _المتطلبات: 11.3_
  - [ ]* 17.6 كتابة اختبار خاصية لحفظ تفضيل الوضع الداكن/الفاتح
    - **الخاصية 29: حفظ تفضيل الوضع الداكن/الفاتح**
    - **يتحقق من: المتطلب 11.3**

- [x] 18. تطوير مكون الإشعارات في الواجهة الويب (المتطلب 9)
  - [x] 18.1 إنشاء `web/client/src/components/NotificationBell.jsx` يعرض عداد الإشعارات غير المقروءة في الـ Header
    - _المتطلبات: 9.2_
  - [x] 18.2 إنشاء `web/client/src/hooks/useNotifications.js` لجلب الإشعارات بـ Polling كل 30 ثانية
    - _المتطلبات: 9.6_
  - [x] 18.3 ربط `NotificationBell` في `web/client/src/Layout.jsx`
    - _المتطلبات: 9.1، 9.2_


- [x] 19. نقطة تفتيش — التحقق من تمرير جميع الاختبارات
  - تأكد من تمرير جميع الاختبارات، اسأل المستخدم إذا ظهرت أي تساؤلات.

- [x] 20. صفحة التذاكر والمهام المتقدمة في الواجهة (المتطلبان 4 و8)
  - [x] 20.1 إنشاء `web/client/src/pages/Tickets.jsx` لعرض تذاكر المستخدم وإنشاء تذاكر جديدة والرد عليها
    - _المتطلبات: 4.1، 4.7_
  - [x] 20.2 تحديث `web/client/src/pages/MyTasks.jsx` لإضافة أزرار Pause/Resume وعرض سجل التدقيق
    - _المتطلبات: 8.3، 8.4، 8.5_
  - [x] 20.3 تحديث `web/client/src/pages/Admin.jsx` لإضافة تبويب إدارة التذاكر مع الفلترة والتعيين
    - _المتطلبات: 4.8_
  - [x] 20.4 تحديث `web/client/src/pages/Wallet.jsx` لعرض خيارات الإيداع والسحب الثلاث مع رسائل الخطأ الواضحة
    - _المتطلبات: 10.1، 10.2، 10.4، 10.5_
  - [x] 20.5 تحديث `web/client/src/App.jsx` لإضافة مسار `/tickets`
    - _المتطلبات: 4.7_

- [x] 21. تحديث لوحة التحكم الرئيسية (المتطلب 7)
  - [x] 21.1 تحديث `web/client/src/pages/Dashboard.jsx` لإضافة الودجات القابلة للتفعيل/الإخفاء مع حفظ التفضيل في localStorage
    - _المتطلبات: 7.1، 7.2_
  - [x] 21.2 إنشاء `web/client/src/hooks/useActivityLog.js` وعرض سجل الأنشطة مع التصفية والـ Pagination في Dashboard
    - _المتطلبات: 7.4، 7.5_
  - [x] 21.3 إضافة مخطط Line Chart بسيط لتطور الرصيد خلال 30 يوماً في Dashboard
    - _المتطلبات: 7.6_


- [ ] 22. اختبارات Round-trip والنماذج الأساسية (المتطلب 13)
  - [x]* 22.1 كتابة اختبار خاصية لـ round-trip لنموذج User (serialize/deserialize)
    - **الخاصية 30: اتساق خاصية round-trip للبيانات**
    - **يتحقق من: المتطلب 13.5**
  - [x]* 22.2 كتابة اختبار وحدة لدوال `User.updateBalance` مع خاصية بقاء الرصيد عند الإضافة ثم الخصم
    - اختبار: `updateBalance(userId, +x)` ثم `updateBalance(userId, -x)` يُعيد الرصيد لقيمته الأصلية
    - **يتحقق من: المتطلب 13.5**
  - [x]* 22.3 كتابة اختبار خاصية لاتساق `completed_count`
    - اختبار: قبول أي تقديم لا يُنقص `completed_count` عن صفر
    - **يتحقق من: المتطلب 13.5**

- [x] 23. نقطة تفتيش نهائية — التحقق من تمرير جميع الاختبارات
  - تأكد من تمرير جميع الاختبارات والتحقق من تكامل المكونات معاً، اسأل المستخدم إذا ظهرت أي تساؤلات.

## ملاحظات

- المهام المُعلَّمة بـ `*` اختيارية ويمكن تخطيها للوصول لـ MVP أسرع
- كل مهمة تُشير إلى متطلبات محددة لضمان التتبع
- نقاط التفتيش تضمن التحقق التدريجي من صحة الكود
- اختبارات الخاصية تستخدم مكتبة `fast-check` بـ 100 تكرار على الأقل
- اختبارات الوحدة تستخدم `jest` مع in-memory SQLite
- جميع الاختبارات يجب أن تكتمل في أقل من 60 ثانية

